# Phase 3 — DDS CDD and RTPS over Virtual AUTOSAR TcpIp

## Result

**STATUS: PASS** (Updated for Phase 4)

Phase 3 validated DDS CDD communication over the Linux virtual AUTOSAR path:

```text
DDS CDD
  -> RTI DDS Micro PIL
  -> RTI AUTOSAR PSL
  -> Virtual AUTOSAR TcpIp
  -> Linux UDP
  -> Windows Host DDS application
```
## Phase4 Handover

Use this section as the starting context for the next agent. The architecture is fixed and must not be redesigned:

```text
Windows Host DDS application
  <-> UDP/RTPS over enp0s8
192.168.56.1 <-> 192.168.56.105
  <-> Linux virtual AUTOSAR TcpIp
  <-> Trampoline POSIX AUTOSAR OS
  <-> RTI AUTOSAR PSL
  <-> RTI DDS Micro PIL
  <-> DDS CDD / RTE runnable
```

Do not switch to QEMU, FreeRTOS, lwIP, ARM, 32-bit, or RTI POSIX PSL. RTI proprietary source and libraries are external and must not be copied into this repository.

### Host and environment

```text
Host OS: Ubuntu Server 26.04.1 LTS
CPU/ABI: x86_64
Compiler: GCC 13.4.0
RTI: Connext DDS Micro 4.3.0 ER738
RTIMEHOME: $HOME/dds/rti_connext_dds_micro-4.3.0_ER738
PIL: $RTIMEHOME/lib/x86_64leElfgcc13.3.0
Trampoline: third_party/trampoline
GOIL target: posix
Linux interface: enp0s8
Linux IP: 192.168.56.105/24
Windows peer: 192.168.56.1
```

Start from:

```bash
cd ~/autosar_virtual
source ./env.sh
```

Normal workflow:

```bash
./generate.sh
./clean_build.sh
./build.sh
```

The Windows Host DDS test application is prepared by the user. Do not modify it. Ask the user to launch it only after the Linux target has been started and its UDP listeners are confirmed.

### Current source ownership

The important project-owned files are:

```text
config/os/autosar_virtual.oil       OIL tasks, alarms, resources, priorities
config/os/stub.c                    main, task bodies, virtual TcpIp polling
platform/autosar/TcpIp.c            Linux-backed AUTOSAR TcpIp shim
platform/autosar/TcpIp.h            TcpIp types/API and diagnostic switch
platform/autosar/Rte_Type.h         RTE data types
platform/autosar/Rte_DdsCddType.c   RTE storage and RX internal trigger
platform/autosar/Rte_DdsCddType.h   RTE declarations
dds_example/autosar_gen/DdsCdd.c    generated runnable implementations
dds_example/adaptation/dds_cdd_adapter.c  DDS CDD/RTI adapter
dds_example/dds_impl/dds_impl.c    participant, writer, reader API calls
dds_example/dds_impl/dds_systemAppgen.c/.h generated AppGen configuration
```

Generated Trampoline files are under `autosar_virtual/` and are regenerated by `generate.sh`. Do not hand-edit them.

### Working OIL configuration

The required resource IDs are intentionally separate:

```oil
RESOURCE OsResource_DdsMain { RESOURCEPROPERTY = STANDARD; };
RESOURCE OsResource_DdsTimer { RESOURCEPROPERTY = STANDARD; };
RESOURCE OsResource_DdsNetio { RESOURCEPROPERTY = STANDARD; };
```

Current working task priorities and ownership:

```text
TcpIp_Task              priority 10; resources DdsNetio, DdsMain
DdsCddTimerTick_Task    priority 5;  resource DdsTimer
DdsCddReadWrite_Task    priority 5;  resources DdsMain, DdsTimer
DdsCddProcessData_Task  priority 1;  resources DdsMain, DdsNetio
RTI_Task                priority 1;  resources DdsMain, DdsTimer
```

`DdsCddProcessData_Task` must be `AUTOSTART = TRUE` and must wait on `DdsCddProcessDataEvent`; otherwise `SetEvent()` returns `E_OS_STATE` because the target task is suspended.

`DdsCddReadWrite_Task` is activated by `DdsCdd100msAlarm`, but the alarm period is now 1000 ms:

```oil
ALARMTIME = 1000;
CYCLETIME = 1000;
TASK = DdsCddReadWrite_Task;
```

### Why both DDS resources are required

The adapter configures:

```c
system_property.psl_property.mutex_resource_id = OsResource_DdsMain;

The Windows Host test application exchanged DDS samples in both directions with the Linux target.
```

The RTI AUTOSAR socket module creates its mutex with `OSAPI_Mutex_new()`, which uses `mutex_resource_id` and therefore `OsResource_DdsMain`. The UDP interface network lock uses `netio_resource_id` and therefore `OsResource_DdsNetio`. `TcpIp_Task` calls into both layers through the RX indication path, so it must own both resources.

### Verified call paths

Socket allocation and bind:

```text
RTI UDP interface
 -> NETIO_AutosarSocket_socket()
 -> OSAPI_System PortProperty.get_socket
 -> DdsCdd_GetSocket()
 -> TcpIp_TcpIp_DdsCddGetSocket()
 -> Linux socket(AF_INET, SOCK_DGRAM, IPPROTO_UDP)
 -> TcpIp_Bind()
 -> Linux bind()
```

RX path:

```text
TcpIp_Task (5 ms)
 -> TcpIp_PollDdsRx()
 -> recvfrom() on retained DDS fd
 -> sockaddr_in -> TcpIp_SockAddrInetType
 -> DdsCdd_RxIndication()
 -> NETIO_Autosar_TcpIp_udp_rx_indication()
 -> RTI receive buffer / dds_rxindication
 -> DdsCddRxIndication()
 -> Trigger_DdsCdd_RxIndication()
 -> Rte_IrTrigger_DdsCdd_RxIndication_ITP_DdsCdd_RxIndication()
 -> SetEvent(DdsCddProcessData_Task, DdsCddProcessDataEvent)
 -> DdsCddProcessData_Task
 -> DdsCddProcessData()
 -> NETIO_Autosar_udp_receive_callback()
 -> RTI NETIO/RTPS processing
```

TX path:

```text
DdsCddReadWrite_Task
 -> DdsCddWrite_Cabin_Door_PDIO_FL()
 -> DdsCdd_Adapter_Write_Cabin_Door_PDIO_FL()
 -> DdsImpl_Cabin_Door_PDIO_FL_WriteSample()
 -> DDS DataWriter write()
 -> RTI UDP transport
 -> NETIO_AutosarSocket_sendto/sendmsg()
 -> TcpIp_UdpTransmit()
 -> Linux sendto()
 -> 192.168.56.1
```

### Address and ABI rules

There is exactly one virtual local address:

```text
LocalAddrId 0 = 192.168.56.105
TCPIP_LOCALADDRID_ANY = 0xFF
```

`TcpIp_GetIpAddr()` and `TcpIp_Bind()` accept LocalAddrId 0; `max_local_addr_id` should not be interpreted as the count of configured addresses.

For IPv4 conversion, preserve `sin_addr.s_addr` as the network-order 32-bit value. Convert only the port with `ntohs()` when constructing the AUTOSAR address for RTI. This matches RTI `NETIO_Autosar_TcpIp_get_sockaddr_in()` and the existing `TcpIp_UdpTransmit()` implementation. The code uses `uint16` SocketIds mapped to small Linux fds; no pointer-to-32-bit conversion is used.

### Runtime verification

Run the target in a PTY so ViPER does not exit because of terminal EOF:

```bash
cd ~/autosar_virtual
source ./env.sh
setsid script -q -f -c './run.sh' /tmp/autosar_pty.out >/dev/null 2>&1 &
```

Confirm listeners:

```bash
ss -lunp | grep -E '192.168.56.105:(8910|8911)'
```

Expected UDP receive sockets are `8910` and `8911`; the DDS send socket uses an ephemeral port. Stop only with:

```bash
pkill -f autosar_virtual
```

`TcpIp_Log()` is retained for future diagnostics but disabled by default:

```c
#define TCPIP_ENABLE_DIAGNOSTIC_LOG 0
```

To enable it for a temporary diagnostic build, change the compile definition or header value deliberately, then restore it after testing. Do not add high-frequency `printf()` calls to the 5 ms task because ViPER raw terminal output becomes corrupted.

### Evidence and PASS state

Verified during Phase3:

```text
SocketOwner callback and Linux socket allocation       PASS
DDS receive bind on 8910 and 8911                     PASS
Nonblocking DDS fd retention and 5 ms polling          PASS
Valid RTPS RX parameters from Windows peer              PASS
RTE SetEvent to ProcessData task                       PASS
RTPS TX through TcpIp_UdpTransmit/sendto               PASS
Windows Host <-> Linux DDS sample exchange              PASS
```

The user confirmed that DDS samples moved in both directions after priority correction. `tcpdump` was not used as final evidence because the environment lacks `CAP_NET_RAW`; application-level DDS exchange and the TcpIp transmit boundary were verified.

### Phase4 starting prompt

Use the following prompt when starting a new agent:

```text
Continue Phase4 in ~/autosar_virtual. Phase3 is PASS and must not be redesigned.
The fixed path is Windows DDS Host 192.168.56.1 <-> Linux 192.168.56.105/enp0s8 <-> virtual AUTOSAR TcpIp <-> Trampoline POSIX <-> RTI AUTOSAR PSL/PIL <-> DDS CDD.
Do not use QEMU, FreeRTOS, lwIP, ARM, 32-bit, or RTI POSIX PSL. RTI proprietary source/libraries stay external.

Phase3 PASS facts:
- SocketOwner reaches DdsCdd_GetSocket and Linux socket allocation.
- DDS sockets bind on UDP 8910 and 8911; send socket is ephemeral.
- TcpIp_Task priority is 10 and owns OsResource_DdsNetio plus OsResource_DdsMain.
- DdsCddProcessData_Task is priority 1, AUTOSTART TRUE, and waits for DdsCddProcessDataEvent.
- DdsCddReadWrite_Task is priority 5 and runs every 1000 ms.
- RTI resources map mutex=DdsMain, timer=DdsTimer, netio=DdsNetio.
- Valid RTPS RX reaches SetEvent and ProcessData; Windows Host and Linux exchanged DDS samples both ways.

Read .ai_note/phase3.md before changing anything. Inspect current git status because the worktree may contain user changes. Use pkill -f autosar_virtual for forced termination. Keep diagnostics in /tmp and avoid printf in the 5 ms task.

Start Phase4 by stating one concrete goal and one PASS criterion, then inspect only the owning code path. Do not re-debug Phase0-3 unless a Phase4 change regresses them.
```

## Validated Items

- RTI SocketOwner callback reached `DdsCdd_GetSocket()`.
- Virtual TcpIp allocated Linux UDP sockets for DDS.
- DDS receive sockets bound through `TcpIp_Bind()` on ports 8910 and 8911.
- DDS send socket bound to an ephemeral local port.
- `TcpIp_Task` retained and polled DDS-owned nonblocking sockets.
- Linux `sockaddr_in` converted to `TcpIp_SockAddrInetType`.
- RX indication reached the RTI AUTOSAR NETIO layer.
- RTE `SetEvent()` activated `DdsCddProcessData_Task`.
- `DdsCddProcessData_Task` processed received RTPS data.
- RTPS traffic was transmitted through `TcpIp_UdpTransmit()` and Linux `sendto()`.
- Windows Host and Linux target exchanged DDS samples successfully.
- Required AUTOSAR resources were configured for RTI mutexes and NETIO synchronization.

## Important Fixes

### Local IP assignment

The virtual target calls:

```c
DdsCdd_LocalIpAddrAssignmentChg(
    0U,
    TCPIP_IPADDR_STATE_ASSIGNED);
```

at the beginning of `RTI_Task`, after `StartOS()` has established the OS context. Calling it before `StartOS()` caused a fault.

### Resource ownership

The RTI AUTOSAR implementation uses multiple resources:

```text
mutex_resource_id -> OsResource_DdsMain
netio_resource_id -> OsResource_DdsNetio
timer_resource_id -> OsResource_DdsTimer
```

The working task configuration gives `TcpIp_Task` both `OsResource_DdsNetio` and `OsResource_DdsMain`. This is required because the RX indication path enters RTI AUTOSAR socket code that takes the main mutex resource.

### RX event task priority

The working priority relationship is:

```text
TcpIp_Task              priority 10
DdsCddTimerTick_Task    priority 5
DdsCddReadWrite_Task    priority 5
DdsCddProcessData_Task  priority 1
```

`DdsCddProcessData_Task` is autostarted and waits for `DdsCddProcessDataEvent`.

### Read/write task

The periodic task was renamed:

```text
DdsCdd100ms_Task -> DdsCddReadWrite_Task
```

Its period is now 1000 ms:

```text
ALARMTIME = 1000;
CYCLETIME = 1000;
```

It performs the DDS timer update, Writer operation, and Reader operation.

## Key Runtime Evidence

SocketOwner and bind path:

```text
DdsCdd_GetSocket -> TcpIp_TcpIp_DdsCddGetSocket
TcpIp_Bind PASS: 192.168.56.105:8910
TcpIp_Bind PASS: 192.168.56.105:8911
```

RTPS transmit boundary:

```text
TcpIp_UdpTransmit
  destination: 192.168.56.1:8910
  length: 440 bytes
  sent: 440 bytes
```

The Windows Host application produced valid RTPS traffic, and the Linux target completed the RX event path. The user confirmed DDS samples moved successfully in both directions after the task-priority adjustment.

## Verification Commands

```bash
cd ~/autosar_virtual
source ./env.sh
./generate.sh
./build.sh
./run.sh
```

Runtime sockets can be checked with:

```bash
ss -lunp
```

The runtime process must be stopped with:

```bash
pkill -f autosar_virtual
```

## Cleanup

Temporary runtime diagnostics were removed from `dds_example`. `TcpIp_Log()` remains available for future diagnostics but is disabled by default:

```c
#define TCPIP_ENABLE_DIAGNOSTIC_LOG 0
```

RTI proprietary source and libraries remain outside the repository. `tcpdump` evidence was not collected because the current environment lacks `CAP_NET_RAW`; the application-level DDS sample exchange was verified with the Windows Host application.

## Remaining Non-blocking Work

- Optional packet capture after granting `CAP_NET_RAW`.
- Optional cleanup of compiler macro redefinition warnings between Trampoline and RTI AUTOSAR headers.
- Optional replacement of the minimal virtual TcpIp shim with additional production-specific AUTOSAR behavior.
